name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  test:
    name: Tests (${{ matrix.platform }})
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 45

    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux/amd64
            runner: ubuntu-latest
          - platform: linux/arm64
            runner: ubuntu-24.04-arm

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build workspace image
        run: |
          docker buildx build \
            --platform ${{ matrix.platform }} \
            --file docker/Dockerfile \
            --target workspace \
            --load \
            --tag cuis-workspace:ci \
            .

      # Apply CoreUpdates to the Smalltalk image.
      # seccomp=unconfined: Squeak JIT requires mprotect(PROT_READ|PROT_EXEC).
      - name: Apply core updates
        run: |
          docker run --rm \
            --security-opt seccomp=unconfined \
            cuis-workspace:ci \
            bash .github/scripts/install-updates.sh

      - name: Run test suite
        run: |
          docker run --rm \
            --security-opt seccomp=unconfined \
            cuis-workspace:ci \
            bash .github/scripts/run-tests.sh
